<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mapper.MessageMapper">
    <insert id="insertList">
        insert into message
        (id,conversation_id, seq, sender_id, type, image,content, status, created_at)
        values
        <foreach collection="messages" item="msg" separator=",">
            (#{msg.id},#{msg.conversationId}, #{msg.seq}, #{msg.senderId}, #{msg.type},  #{msg.image},#{msg.content}, #{msg.status}, #{msg.createdAt})
        </foreach>
    </insert>

</mapper>